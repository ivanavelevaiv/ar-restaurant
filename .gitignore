node_modules/
.DS_Store
Thumbs.db

# 16 MB original backup — keep only the optimised working model in the repo
models/shrimp-pasta-original.glb

# Raw source assets in the project root (processed copies live under images/)
*.JPG
*.PNG

# Blender source/output files — raw assets that belong in a separate asset pipeline
*.fbx
*.obj
# Root-level GLB exports from Blender (web-ready models live under models/)
/*.glb
